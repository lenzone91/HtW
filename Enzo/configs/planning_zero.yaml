logging:
  tqdm_silent: false

task_specification:
  goal_source: random_state
  obs: rgb

planner:
  planner_name: custom
  planner_target: Enzo.planners.zero.ZeroPlanner
  num_act_stepped: 1
  plan_length: 90
  action_dim: 2
  decode_each_iteration: false
  planning_objective:
    objective_type: repr_dist
    sum_all_diffs: true
    alpha: 0.

ctxt_window_time: 1
