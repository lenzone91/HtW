"""
AcVideoJEPA data: the two-rooms dataset adapter and the ac_video_jepa collator
(registered onto the AIML data registries), plus the vendored `two_rooms`
package. DataLoader assembly uses AIML's generic `DefaultDataModule` (composed
via config); there is no experiment-specific datamodule.
"""
