# Entry point for the `toy_run` run. Composes the sibling fragments via Hydra.
# Each fragment owns one config section. The merged result is the run config
# (see ../toy_run.yaml for the composed snapshot).
defaults:
  - setup
  - datamodule
  - module
  - trainer
  - loggers
  - run
  - _self_
