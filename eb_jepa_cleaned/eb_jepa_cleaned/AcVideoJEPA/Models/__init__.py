"""
AcVideoJEPA models: the experiment's neural-network architecture and Lightning
runtime.

Subpackages:
- `Backbones/` — encoder, predictor, projector, inverse-dynamics building blocks;
- `Rollout/` — latent rollout (autoregressive / parallel) and its output;
- `Modules/` — the registered AcVideoJEPA Lightning module (the JEPA step).
"""
