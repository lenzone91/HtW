# Workflow README

This folder owns shared workflow-level utilities.

`Factory/` contains the registry-based factory foundation used to remove
duplicated factory dispatch and validation logic from subsystem factories.

`Utils/` contains small shared helpers that do not belong to a domain subsystem.
